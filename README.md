# 🚀 ESTODOLIST

## Sobre o Projeto

O ESTODOLIST é uma aplicação simples e responsiva de Lista de Tarefas (To-Do List) desenvolvida com o objetivo de praticar o desenvolvimento Front-end.

O projeto foca na manipulação do DOM e no gerenciamento de estado de forma local, utilizando o `localStorage` do navegador para persistência dos dados.

## ✨ Funcionalidades

O projeto oferece um conjunto essencial de recursos para gerenciamento de tarefas:

* **Adicionar Tarefa:** Crie novas tarefas com título, descrição e data de vencimento.
* **Visualizar Lista:** Exibe todas as tarefas de forma clara e organizada.
* **Marcar como Concluída:** Altera o status da tarefa para `Concluída` com um simples clique no *checkbox*.
* **Editar Tarefa:** Permite modificar o título, a descrição, a data de vencimento e o status da tarefa através de um modal.
* **Excluir Tarefa:** Remove tarefas permanentemente da lista.
* **Filtrar Tarefas:** Filtra a visualização com base no status (`Todas`, `Pendente`, `Concluída`, `Cancelada`).
* **Pesquisar Tarefas:** Permite buscar tarefas por título ou descrição.
* **Tema:** Alternância entre os modos **Claro** e **Escuro** (Dark Mode).

## 💻 Tecnologias

As seguintes tecnologias foram utilizadas para o desenvolvimento do projeto:

| Categoria | Tecnologia | Objetivo |
| :--- | :--- | :--- |
| **Estrutura** | HTML5 | Estrutura semântica da aplicação. |
| **Estilização** | Tailwind CSS | Framework CSS para o desenvolvimento rápido e responsivo da interface. |
| **Lógica** | JavaScript (ES6+) | Lógica de negócios, manipulação do DOM e persistência de dados. |
| **Persistência** | `localStorage` | Armazenamento local das tarefas no navegador. |

## 🛠 Como Usar

Para rodar este projeto localmente, siga os passos abaixo:

1.  **Clone o repositório:**
    ```bash
    
    git clone isabelly-ferreira-lima/Projeto-EsToDoList
    
    ```
2.  **Navegue até o diretório do projeto:**
    ```bash
    cd estodolist
    ```
3.  **Abra o arquivo `index.html`:**
    * Simplesmente clique duas vezes no arquivo `index.html` em seu explorador de arquivos, ou;
    * Abra-o diretamente no seu navegador de preferência.

As tarefas serão salvas automaticamente no `localStorage` do seu navegador.

## 🧭 NAVEGE NO ESTODOLIST
https://es-to-do-list-peach.vercel.app
